.venv/
__pycache__/
*.pyc
*.egg-info/
.uv/
.pytest_cache/
.ruff_cache/
dist/
build/
.DS_Store

# WhyGraph per-repo cache (evidence + rationale SQLite).
.whygraph/

# CodeGraph per-repo cache (symbol graph SQLite + config).
.codegraph/

# Local copy of the dev compose file (template: docker-compose.example.yml).
/docker-compose.yml

# Personal Claude Configurations
.claude/settings.local.json
